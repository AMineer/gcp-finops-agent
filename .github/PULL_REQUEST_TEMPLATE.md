## Summary

What does this PR do and why?

## Changes

- 

## Testing

How was this tested? Check all that apply:

- [ ] Unit tests pass (`uv run pytest -m "not eval" -v`)
- [ ] Type check passes (`uv run mypy gcp_finops_agent`)
- [ ] Lint passes (`uv run ruff check .`)
- [ ] Tested locally with `adk web`
- [ ] Tested against real GCP billing data

## Notes for reviewer

Anything the reviewer should know — edge cases, follow-ups, areas of uncertainty.
